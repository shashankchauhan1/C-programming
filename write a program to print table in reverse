// program to print table in reverse.

#include<stdio.h>

int tab(int n);

int main() {
    int n;
    printf("enter a number: ");
    scanf("%d",&n);
    tab(n);
    return 0;
}

int tab(int n){
    for(int i=10;i>=1;i--){
        printf("%d * %d = %d\n",n,i,n*i);
    }
    return 0;
}
