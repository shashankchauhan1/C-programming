1> without functions.


#include<stdio.h>
#include<string.h>
int main() {
    char name[100];
    printf("enter your password: ");
    fgets(name,100,stdin);
    char sname[100];
    char salt[100]="123";
    strcpy(sname,name);
    strcat(sname,salt);
    puts(sname);
    return 0;
}
