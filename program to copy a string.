// program to copy a string.

#include<stdio.h>
int main() {
    char str1[100]="shashank";
    char str2[100];
    for(int i=0; str1[i]!='\0'; i++){
        str2[i]=str1[i];
    }
    puts(str1);
    puts(str2);
    return 0;
}
