// write a program to check weather the given character is alphatic character or not(A - Z) (a - z).

Q write a program to check the alphatic character?

#include<stdio.h>
#include<ctype.h>
int main() {
    char ch;
    printf("enter a character: ");
    scanf("%c",&ch);
    if(isalpha(ch)){
        printf("it is a alphatic character");
    }
    else{
        printf("it is not a alphatic character");
    }
    return 0;
}
