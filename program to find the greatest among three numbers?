// write a program to find the greatest among three numbers?

#include<stdio.h>
int main() {
    int a,b,c;
    printf("enter a: ");
    scanf("%d",&a);
    printf("enter b: ");
    scanf("%d",&b);
    printf("enter c: ");
    scanf("%d",&c);
    if(a>b && a>c){
        printf("greatest numebr is: %d",a);
    }
    else if(b>a && b>c){
        printf("greatest number is: %d",b);
    }
    else if(c>a && c>b){
        printf("greatest number is: %d",c);
    }
    return 0;
}
