// program to calculate area of square formula: (side*side).

# include<stdio.h>
int main() {
    int side;
    
    printf("enter the side of square: ");
    scanf("%d",&side);
    
    printf("this is the area of the square : %d",side*side);
    return 0;
}
