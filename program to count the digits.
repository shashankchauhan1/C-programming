// program to count digits in a number.


#include<stdio.h>
int main(){
    int n,digit;
    printf("enter a number: ");
    scanf("%d",&n);
    while(n!=0){
        n=n/10;
        digit++;
    }
    printf("total number of digits: %d",digit);
    return 0;
}
