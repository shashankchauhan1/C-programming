// program to check the input is digit or not.

#include<stdio.h>
#include<ctype.h>
int main() {
    char ch;
    printf("enter a digit: ");
    scanf("%c",&ch);
    if(isdigit(ch)){
        printf("it is a digit.");
    }
    else{
        printf("it is not a digit.");
    }
    return 0;
}
